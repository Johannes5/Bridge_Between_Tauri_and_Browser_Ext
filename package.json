{
  "name": "bridge-workspace",
  "version": "0.0.1",
  "private": true,
  "workspaces": ["packages/*"],
  "scripts": {
    "setup": "yarn install",
    "build:proto": "yarn workspace shared-proto build",
    "dev:app": "yarn workspace mapmap-test-app tauri dev",
    "dev:ext": "yarn workspace shortstop run dev",
    "build:ext": "yarn workspace shortstop run build && yarn workspace shortstop run package "
  },
  "devDependencies": {
    "@modelcontextprotocol/sdk": "^1.20.1",
    "@playwright/test": "^1.49.0",
    "@types/node": "^20.16.5",
    "ts-node": "^10.9.2",
    "tslib": "^2.7.0",
    "typescript": "^5.6.3",
    "ws": "^8.18.0",
    "zod": "^3.23.8"
  }
}
